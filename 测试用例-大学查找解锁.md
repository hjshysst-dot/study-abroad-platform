# 测试用例：大学查找按钮解锁逻辑

- **用例编号**：TC-大学查找-001
- **测试目标**：「大学查找」按钮的锁定/解锁机制
- **优先级**：高
- **创建日期**：2026-04-21

---

## 依赖关系确认（源码）

```
学生信息 → 多国策略 → 大学查找 → 选校维度 → Offer决策 → 费用计算
              ↓
           费用计算（也依赖多国策略）
```

```javascript
// App.tsx 第408-418行
const dependencies: Record<string, string[]> = {
  strategy: ['profile'],
  timeline: ['strategy'],
  finder: ['strategy'],      // ← 大学查找依赖多国策略
  dimensions: ['finder'],
  decision: ['dimensions'],
  cost: ['strategy'],
};
```

---

## 测试步骤

### 场景1：初始状态验证（不完成前置页面）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 启动平台 `npm run dev`，访问 http://localhost:3000 | 页面正常加载，显示「留学Agent平台 v2.1」 |
| 2 | 确认当前 Tab 是「学生信息」（默认） | Tab 高亮显示「学生信息」 |
| 3 | 直接点击左侧菜单的「大学查找」按钮 | 按钮灰色（disabled），弹出提示「请先完成【多国策略】页面」 |
| 4 | 检查按钮样式 | 存在 `cursor-not-allowed` + `opacity-50` CSS class |
| 5 | 悬浮在「大学查找」按钮上 | 鼠标指针变为 not-allowed，提示🔒图标 |

---

### 场景2：逐步解锁验证

#### Step 1：完成「学生信息」

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在「学生信息」Tab 选择申请类型 | 可选：本科/硕士/博士 |
| 2 | 选择「本科」 | 页面刷新显示本科表单 |
| 3 | 填写任意测试数据（如 GPA、SAT/ACT、目标国家） | 表单字段正常输入 |
| 4 | 点击「生成选校策略」或类似提交按钮 | 数据提交成功，触发 `setPageCompletion('profile', true)` |
| 5 | 检查页面顶部「学生信息」Tab 旁 | 显示 ✅绿色勾选图标 |

#### Step 2：检查「多国策略」是否解锁

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 观察左侧 Tab 菜单 | 「多国策略」Tab 变为可点击（不再是灰色） |
| 2 | 点击「多国策略」Tab | 页面切换到「多国策略」Tab |
| 3 | 确认「大学查找」仍然禁用 | 仍然是灰色+disabled，因为 `dependencies['finder']=['strategy']`，`strategy` 还未完成 |

#### Step 3：完成「多国策略」

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在「多国策略」Tab 填写策略数据（如目标国家：美/英/港/新/澳） | 表单正常填写 |
| 2 | 点击「生成策略」或提交按钮 | 策略生成成功，触发 `setPageCompletion('strategy', true)` |
| 3 | 检查页面顶部「多国策略」Tab 旁 | 显示 ✅绿色勾选图标 |
| 4 | 观察「大学查找」Tab | 按钮变为**可点击**状态（蓝色高亮或正常颜色），🔒图标消失 |

#### Step 4：验证「大学查找」正式解锁

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击「大学查找」Tab | 页面切换到「大学查找」页面，正常显示 |
| 2 | 确认 URL 或 Tab 状态 | 「大学查找」Tab 高亮，URL 或内容反映当前 Tab |
| 3 | 检查「选校维度」Tab 状态 | 「选校维度」仍然禁用（依赖 `finder`，而当前 `finder` 刚完成未记录 completion） |

---

### 场景3：边界条件测试

| 用例 | 操作 | 预期结果 |
|------|------|---------|
| B-1 | 完成「学生信息」后，直接刷新页面 | `pageCompletion` 状态是否持久化？（可能丢失，待确认） |
| B-2 | 完成「多国策略」后，清除浏览器缓存，刷新 | 状态是否从服务器重新获取？ |
| B-3 | 完成「多国策略」后，尝试通过代码直接访问被锁定的 Tab | 是否有前端路由保护？ |

---

## 测试数据模板

### 学生信息填写

```
申请类型：本科
目标学位：Bachelor
目标国家（多选）：美国、英国、香港、新加坡、澳大利亚
专业方向：Computer Science / Engineering
```

### 多国策略填写

```
申请策略：美国（主申）、英国（稳妥）、香港（保底）
申请数量：美国15所 + 英国5所 + 香港3所
预算范围：50-80万人民币/年
```

---

## 通过标准

- [ ] 场景1：初始状态「大学查找」显示为 disabled，悬浮显示🔒
- [ ] 场景2 Step3：完成「多国策略」后，「大学查找」立即变为 enabled
- [ ] 场景2 Step4：点击「大学查找」可正常进入页面
- [ ] 场景3：边界条件符合预期

---

## 实际测试记录

（下方由测试者填写）

| 测试日期 | 测试者 | 结果 | 备注 |
|---------|--------|------|------|
| 2026-04-21 | 小运 | 待测 | |
